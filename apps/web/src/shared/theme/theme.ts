/**
 * Theme re-export.
 *
 * The authoritative MUI theme lives in `design-system/theme/theme.ts`.
 * Import from there directly, or use this re-export for convenience:
 *
 *   import theme from '@/shared/theme/theme';
 */

export { default } from '../../../../../design-system/theme/theme';
